{
    "navigationFallback": {
        "rewrite": "/.auth/login/github"
    },
    "routes": [
        {
          "route": "/*",
          "allowedRoles": ["authenticated"]
        }
    ],
    "responseOverrides": {
        "401": {
          "rewrite": "/login.html"
        }
      }
}